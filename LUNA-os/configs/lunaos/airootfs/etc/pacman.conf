#
# /etc/pacman.conf - Live Environment
#
# See the pacman.conf(5) manpage for option and repository directives

[options]
Architecture = auto
Color
CheckSpace
SigLevel = Required DatabaseOptional
ParallelDownloads = 5

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

[multilib]
Include = /etc/pacman.d/mirrorlist
